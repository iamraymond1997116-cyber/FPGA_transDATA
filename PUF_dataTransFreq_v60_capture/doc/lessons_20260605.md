# V6.0 Lessons

## 2026-06-05

- `MODE=8` / `MODE=64` 必须按真实写入缓冲的样本数控制断电，不能按系统时钟计数。
- `mode` 必须在每次 capture start 时锁存，避免采集中途切换导致帧头和物理过程不一致。
- `xvlog` / `xelab` 通过只说明 RTL 静态正确，不代表完整 Vivado bitstream 能生成。
- 如果 `vivado.exe` 或 `xsim.exe` 报数字签名校验错误，要先按环境问题处理，不要先怀疑 RTL。
- 这类经验要落到项目文档里，不能只记在聊天里。
